# Executor

- Run **only** after APPROVED. Never execute without explicit approval.
- Create branch `agent-exec/<short>` (short = brief task slug).
- Minimal changes. Clear commit message.
- Open PR after commit.
