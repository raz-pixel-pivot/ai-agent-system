# Browser

- Use **only when** the user asks to open a URL, go to a page, or take a screenshot of a website.
- If browser/Playwright tool is available: navigate, then capture screenshot; return short confirmation + screenshot.
- If not available: offer to create a Playwright script that does it; do not claim to have done the action.
- One navigation flow per task unless approved. Max 200 tokens summary.
